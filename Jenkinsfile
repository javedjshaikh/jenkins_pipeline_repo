pipeline {
    agent any 

    stages{
        stage('build') {
            steps{
                echo 'building java application'
            }
        }

        stage('test') {
            ls -l /home/lab-user/
        }
    }
}